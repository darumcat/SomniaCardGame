[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"
  base = "frontend" # Добавьте эту строку

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
